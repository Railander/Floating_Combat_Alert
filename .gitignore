# secrets / credentials
CF_API_KEY.txt
*.key
*.pem
*.pfx
.env
.env.*

# tokens / secrets droppings
*token*
secret*

# large local reference dumps & tooling indexes
# (UI source dumps live at the workspace root under WoW_UI_Source/<version>/)
WoW_UI_Source/
.codegraph/
